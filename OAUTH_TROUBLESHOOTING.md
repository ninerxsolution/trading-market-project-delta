# แก้ปัญหา OAuth ยังใช้งานไม่ได้

## 🔍 ตรวจสอบ Configuration

### 1. ตรวจสอบ Configuration
ไปที่: `https://2d7a810b4f87.ngrok-free.app/api/auth/roblox/test`

Endpoint นี้จะแสดง:
- Environment variables ที่ใช้
- Redirect URI ที่ตั้งไว้
- ตรวจสอบว่าตรงกันหรือไม่

### 2. ตรวจสอบ ngrok Tunnel
ตรวจสอบว่า ngrok URL ยังเป็น `https://2d7a810b4f87.ngrok-free.app` หรือไม่:
- ถ้าเปลี่ยน ต้องอัปเดต Roblox Dashboard และ `.env.local`

### 3. ตรวจสอบ Roblox Dashboard
แม้ว่าจะบอกว่า redirect URI ตั้งไว้ถูกแล้ว แต่ต้องตรวจสอบว่า:
- Redirect URI ใน Roblox Dashboard **ตรงกันเป๊ะๆ** (ไม่มีการ trailing slash หรือความแตกต่างเล็กน้อย)
- App อยู่ในสถานะ "In Review" - **อาจจะยังใช้ OAuth ไม่ได้** จนกว่า review จะเสร็จ

## ⚠️ ปัญหาที่เป็นไปได้

### 1. App อยู่ในสถานะ "In Review"
**ปัญหา**: Roblox อาจจะไม่อนุญาตให้ใช้ OAuth จนกว่า review จะเสร็จ

**แก้ไข**:
- **ทางเลือก 1**: รอให้ review เสร็จ (ประมาณ 5 วัน)
- **ทางเลือก 2**: สร้าง OAuth app ใหม่เพื่อทดสอบ (ไม่ submit)

### 2. Redirect URI ไม่ตรงกัน
**ปัญหา**: แม้ว่าจะตั้งไว้แล้ว แต่อาจจะไม่ตรงกันเป๊ะๆ

**ตรวจสอบ**:
- ไปที่ Roblox Dashboard
- ดู Redirect URI ที่ตั้งไว้
- ต้องตรงกับ `https://2d7a810b4f87.ngrok-free.app/api/auth/roblox/callback` **เป๊ะๆ**

### 3. Ngrok URL เปลี่ยน
**ปัญหา**: ngrok URL เปลี่ยนทุกครั้งที่ restart

**แก้ไข**:
- ตรวจสอบ ngrok URL ปัจจุบัน
- อัปเดต Roblox Dashboard และ `.env.local`
- หรือใช้ ngrok paid plan เพื่อได้ static URL

## 🧪 วิธีทดสอบ

### Step 1: ตรวจสอบ Configuration
1. ไปที่: `https://2d7a810b4f87.ngrok-free.app/api/auth/roblox/test`
2. ดูว่ามี warning หรือ error อะไร

### Step 2: ตรวจสอบ Browser Console
1. เปิด browser console (F12)
2. ลอง OAuth flow
3. ดู error messages

### Step 3: ตรวจสอบ Server Logs
1. ดู terminal ที่รัน `npm run dev`
2. ดู error messages เมื่อ OAuth callback ถูกเรียก

### Step 4: ทดสอบ OAuth Flow
1. ไปที่: `https://2d7a810b4f87.ngrok-free.app/login`
2. คลิก "Login with Roblox"
3. ดู error message ที่แสดง

## 🔧 วิธีแก้ไข

### วิธีที่ 1: สร้าง OAuth App ใหม่ (แนะนำสำหรับทดสอบ)

1. ไปที่: https://create.roblox.com/dashboard/credentials
2. สร้าง OAuth app ใหม่ (ไม่ submit)
3. ตั้งค่า:
   - Entry Link: `https://2d7a810b4f87.ngrok-free.app`
   - Privacy Policy: `https://2d7a810b4f87.ngrok-free.app/privacy`
   - Terms: `https://2d7a810b4f87.ngrok-free.app/terms`
   - Redirect URI: `https://2d7a810b4f87.ngrok-free.app/api/auth/roblox/callback`
   - Scopes: `openid`, `profile`
4. **ไม่ต้อง submit** - ใช้สำหรับทดสอบ
5. อัปเดต `.env.local` ด้วย Client ID และ Secret ใหม่

### วิธีที่ 2: รอให้ Review เสร็จ

- รอประมาณ 5 วัน
- หลังจาก review เสร็จ OAuth ควรจะใช้งานได้

### วิธีที่ 3: ตรวจสอบ Error Messages

1. ดู error message ที่แสดงใน login page
2. ดู error ใน browser console
3. ดู error ใน server logs
4. ใช้ข้อมูลนี้เพื่อแก้ไข

## 📋 Checklist

- [ ] ตรวจสอบ configuration (`/api/auth/roblox/test`)
- [ ] ตรวจสอบ ngrok URL ยังถูกต้อง
- [ ] ตรวจสอบ redirect URI ใน Roblox ตรงกันเป๊ะๆ
- [ ] ตรวจสอบ browser console สำหรับ errors
- [ ] ตรวจสอบ server logs สำหรับ errors
- [ ] ถ้ายังไม่ได้ ลองสร้าง OAuth app ใหม่

## 🎯 Next Steps

1. ไปที่ `/api/auth/roblox/test` เพื่อดู configuration
2. ตรวจสอบ error messages ที่แสดง
3. ถ้า app อยู่ใน review อาจจะต้องรอ หรือสร้าง app ใหม่เพื่อทดสอบ

