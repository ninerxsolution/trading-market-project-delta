# แก้ปัญหา: เพิ่ม Scopes ไม่ได้ใน Roblox Dashboard

## 🔍 สาเหตุที่เป็นไปได้

### 1. App ถูก Submit แล้ว
- ถ้า app อยู่ในสถานะ "Submitted" หรือ "Under Review" อาจแก้ไขไม่ได้
- ต้องรอให้ Roblox อนุมัติหรือ reject ก่อน
- หรือสร้าง OAuth app ใหม่

### 2. ต้องกรอกข้อมูลอื่นก่อน
- บางครั้งต้องบันทึกข้อมูลพื้นฐานก่อน (เช่น Description, Entry Link)
- แล้วค่อยกลับมาเพิ่ม scopes

### 3. UI Bug
- ลองรีเฟรชหน้า (F5)
- ลองใช้เบราว์เซอร์อื่น
- ลองลบ cache และ cookies

### 4. Scopes ไม่มีในระบบ
- Roblox อาจมี scopes ที่จำกัด
- ลองใช้ `openid` เพียงอย่างเดียวก่อน

## ✅ วิธีแก้ไขทีละขั้นตอน

### ขั้นตอนที่ 1: ตรวจสอบสถานะ App
1. ดูว่า OAuth app อยู่ในสถานะอะไร
2. ถ้าเป็น "Draft" หรือ "Edit" ควรแก้ไขได้
3. ถ้าเป็น "Submitted" หรือ "Published" อาจแก้ไขไม่ได้

### ขั้นตอนที่ 2: บันทึกข้อมูลพื้นฐานก่อน
1. กรอก Description
2. กรอก Entry Link (HTTPS)
3. กรอก Privacy Policy URL
4. กรอก Terms of Service URL
5. **บันทึก (Save)**
6. กลับมาเพิ่ม scopes

### ขั้นตอนที่ 3: เพิ่ม Scopes
1. ไปที่ส่วน "Permissions" หรือ "Scopes"
2. คลิกที่ช่อง "Search scopes"
3. พิมพ์ `openid` (พิมพ์ช้าๆ รอให้ dropdown ปรากฏ)
4. **คลิก** ที่ `openid` จาก dropdown (อย่ากด Enter)
5. ทำซ้ำกับ `profile`

### ขั้นตอนที่ 4: ตรวจสอบ
- ดูว่ามี `openid` และ `profile` ในรายการ "Selected Scopes" หรือไม่
- ถ้ามีแล้ว บันทึกอีกครั้ง

## 🎯 ทางเลือกอื่น

### ทางเลือก 1: สร้าง OAuth App ใหม่
1. สร้าง OAuth app ใหม่ใน Roblox Dashboard
2. กรอกข้อมูลทั้งหมดพร้อม scopes
3. ใช้ Client ID และ Client Secret ใหม่

### ทางเลือก 2: ใช้ `openid` เพียงอย่างเดียว
- ลองใช้แค่ `openid` scope
- อาจจะพอสำหรับการทดสอบเบื้องต้น
- อัปเดตโค้ดเป็น: `scope=openid`

### ทางเลือก 3: ติดต่อ Roblox Support
- ถ้ายังแก้ไขไม่ได้
- ติดต่อ Roblox Developer Support
- อธิบายปัญหาที่เจอ

## 📝 Checklist

- [ ] ตรวจสอบสถานะ OAuth app (ต้องเป็น Draft/Edit)
- [ ] กรอกข้อมูลพื้นฐานทั้งหมด
- [ ] บันทึกข้อมูลพื้นฐาน
- [ ] พยายามเพิ่ม `openid` scope
- [ ] พยายามเพิ่ม `profile` scope
- [ ] บันทึกอีกครั้ง
- [ ] ทดสอบ OAuth flow

## ⚠️ หมายเหตุ

- บางครั้ง Roblox Dashboard อาจมี bug
- ลองใช้เบราว์เซอร์อื่น (Chrome, Firefox, Edge)
- ลองในโหมด Incognito/Private
- ถ้ายังไม่ได้ อาจต้องรอ Roblox อัปเดตระบบ

